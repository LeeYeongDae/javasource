package oop;

public class Card {
     // 멤버 변수, 멤버 메소드, 생성자
     String shape;
     char number;

     // 클래스 변수
     static int width = 100;
     static int height = 250;

     // 생성자 : 명시하지 않으면 default 생성
     public Card() {
     } // default 생성자

}
